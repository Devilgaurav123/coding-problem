def find_duplicate(arr):
    df=set()
    duplicate=[]
    for num in arr:
        duplicate.append(num)
        if df in num:
        else:
            df.add(num)

    return duplicate
list = [1, 2, 3, 5, 6, 7, 8, 7, 5, 3, 1]
result = find_duplicate(list)
print("duplicate value is={}".format(result))
